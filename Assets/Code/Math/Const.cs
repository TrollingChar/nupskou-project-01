using UnityEngine;


namespace Code {

    public class Const {

        public static readonly float phi = Mathf.Sqrt(1.25f) - 0.5f;
        public static readonly float Phi = Mathf.Sqrt(1.25f) + 0.5f;
        public static readonly float phiAngle = 2 * Mathf.PI * phi;

    }

}
